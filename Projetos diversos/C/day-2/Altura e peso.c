#include <stdio.h>

int main() {
    float altura = 1.75; // Atribua um valor para altura
    float peso = 70.0;   // Atribua um valor para peso

    printf("Altura: %.2f metros\n", altura);
    printf("Peso: %.2f kg\n", peso);

    return 0;
}